import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        mintyGreen: '#5DC48D',
        mintyGreenBorder: '#11AF22',
        white: '#ffffff'
    }
})

export default theme;